# screenshot_translation_tool
This project provides a screenshot tool that allows the user to select an area on the screen, capture the screenshot, extract the text from it using OCR (Optical Character Recognition), and then translate the extracted text into a target language using the DeepL API
